#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
// the logic is basically sorting inputs as per start,end along with their heights
// then maintain a max heap of the size found so far along along with their start and end (thats where the problem starts)
//then go through find it and print
int main(){
    int n;
    scanf("%d",&n);

return 0;
}